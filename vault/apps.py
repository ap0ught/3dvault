from django.apps import AppConfig


class VaultConfig(AppConfig):
    """AppConfig for the 'vault' application.

    This class configures application-specific settings for the 'vault' app.
    """
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'vault'
